# E03 — Recalculate a versioned personal model

**Status:** [ ] Not started
**Domain:** `effective_paths`
**Dependencies:** [E01](./E01.md), [E02](./E02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Implement a connector to an authorized personal workbook or a compatible validated local calculation engine. Detect formula errors, external IDS dependencies, calculation completion and output freshness.

## Acceptance gate

A calculation result names both workbook and account revision, matches golden cases, and is rejected if inputs changed or dependent formulas are still stale.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `effective_paths_contract.py`
- Inspect/modify: `effective_paths/inputs.py`
- Create when absent: `effective_paths/connector.py`
- Focused verification: `tests/test_effective_paths_connector.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/e03-recalculate-a-versioned-personal-model` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_effective_paths_connector.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_connector.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `effective_paths_contract.py` and `effective_paths/connector.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_connector.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/e03-recalculate-a-versioned-personal-model`, and open a PR against `main`.
