# L01 — Read labs, slots and research timers

**Status:** [ ] Partial — the state model landed; no lab has been read off a real screen
**Domain:** `labs`
**Dependencies:** [B03](./B03.md), [B04](./B04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read lab ownership, unlocked research, exact levels/costs/durations, active jobs, queues, speeds and acceleration state; model unlock milestones.

## Acceptance gate

All owned lab slots and active completions survive a restart; an unseen lab is not assumed unresearched.

## Current repository state

- [ ] No graph-level implementation has been certified complete.
- Landed slice: `labs.py` holds the Lab state model — `LabEntry`, `LabJob`,
  `LabsReading` and `LabsState` — bounded to the catalog's 158 `labs`
  concepts, with `unknown`, `locked`, `unavailable`, `maxed` and `unreadable`
  kept apart and a two-frame confirmation window before anything is written.
- Landed slice: `account_state.AccountRevision` gained `lab_jobs` and
  `lab_slots_owned`, and `AccountState.record_labs` merges confirmed levels,
  replaces jobs only when the whole slot strip was read, and leaves stored
  answers standing when a frame carries no evidence.
- Landed slice: `screen_discovery` declares `labs_screen_layout` (B08),
  `labs_research_actions` (L02) and `labs_acceleration_spend` (L03)
  unsupported. `discover` still refuses a `labs` context outright.
- NOT landed: there is no Labs reader. No Labs capture exists in
  `tests/fixtures/` at any unlock stage, so no geometry, OCR anchor or tap
  target could be measured, and the outcome's "read … exact levels/costs/
  durations, active jobs, queues, speeds and acceleration state" is
  unproven against a real screen. Every focused check drives the model with
  constructed observations.
- Deferred: adding a root-level module moves `runtime_identity.source_hash()`,
  so `web/static/.build-manifest.json` is stale on this branch by design. The
  tracked UI is rebuilt once, centrally, after the parallel labs/cards/modules
  /UW branches merge; rebuilding on each of them would be four wrong answers.
- NOT landed: unlock milestones are modelled but empty. The catalog records
  `prerequisites: null` for all 158 labs, so `unlock_milestone` reports
  `unknown` for every one of them and will only say more once the catalog does.

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `screen_discovery.py`
- Create when absent: `labs.py`
- Focused verification: `tests/test_labs.py`

## Execution checklist

- [x] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [x] Create branch `codex/l01-read-labs-slots-and-research-timers` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [x] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [x] Add failing tests in `tests/test_labs.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [x] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_labs.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [x] Implement the smallest complete change in `account_state.py` and `labs.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [x] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_labs.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [x] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/l01-read-labs-slots-and-research-timers`, and open a PR against `main`.
