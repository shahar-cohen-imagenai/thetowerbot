# O03 — Explain each plan and purchase

**Status:** [ ] Partial — resume
**Domain:** `operations`
**Dependencies:** [E06](./E06.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Extend live decisions into a dependency view: current goal, next action, alternatives, blockers, cost, source version and expected/observed result.

## Acceptance gate

A displayed decision can be replayed from its account/policy/workbook revisions, including why a cheaper-looking alternative was not chosen.

## Current repository state

- [x] The control room displays current decisions and advisor data.
- [ ] Still required: revision-linked alternatives, blockers, cost, expected outcome, observed outcome, and replayable rationale.

## File map

- Inspect/modify: `advisor.py`
- Inspect/modify: `planner.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `web/ui/components/AdvisorPanel.tsx`
- Focused verification: `tests/test_advisor.py`
- Focused verification: `web/ui/components/AdvisorPanel.test.tsx`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/o03-explain-each-plan-and-purchase` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_advisor.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_advisor.py -q`; then run `npm test --prefix web/ui -- --run web/ui/components/AdvisorPanel.test.tsx` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `advisor.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_advisor.py -q`; then run `npm test --prefix web/ui -- --run web/ui/components/AdvisorPanel.test.tsx` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/o03-explain-each-plan-and-purchase`, and open a PR against `main`.
