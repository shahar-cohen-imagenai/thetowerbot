# V05 — Certify optimizer-led weekly operation

**Status:** [ ] Not started
**Domain:** `validation`
**Dependencies:** [E07](./E07.md), [L03](./L03.md), [T01](./T01.md), [T02](./T02.md), [O02](./O02.md), [O04](./O04.md), [V02](./V02.md), [V04](./V04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Run a seven-day soak for the selected qualified account capabilities spanning resets, research turnover and available scheduled activities; each newly enabled domain requires its own fixture and live evidence before inclusion.

## Acceptance gate

Zero routine intervention across the declared enabled scope; all covered recommendation types execute and recalculate correctly; exclusions are explicit and cannot be called full game completeness.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `planner.py`
- Inspect/modify: `labs.py`
- Inspect/modify: `missions.py`
- Inspect/modify: `qualification/overnight.py`
- Create when absent: `qualification/weekly.py`
- Focused verification: `tests/test_weekly_qualification.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v05-certify-optimizer-led-weekly-operation` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_weekly_qualification.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_weekly_qualification.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `planner.py` and `qualification/weekly.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_weekly_qualification.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v05-certify-optimizer-led-weekly-operation`, and open a PR against `main`.
