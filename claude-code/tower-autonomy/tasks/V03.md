# V03 — Prove recovery under faults

**Status:** [ ] Not started
**Domain:** `validation`
**Dependencies:** [B06](./B06.md), [F04](./F04.md), [F08](./F08.md), [V01](./V01.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Inject process loss before/after taps, ADB loss, unknown overlays, missing UI replies, stale calculator output, full queues and resource races.

## Acceptance gate

Every scenario reaches an explicit recover/skip/blocked outcome within its configured bound; no duplicate spending or silent data corruption occurs.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `supervisor.py`
- Inspect/modify: `maintenance.py`
- Inspect/modify: `interruptions.py`
- Inspect/modify: `tests/capability_matrix.json`
- Create when absent: `tests/faults.py`
- Focused verification: `tests/test_recovery.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v03-prove-recovery-under-faults` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_recovery.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_recovery.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `supervisor.py` and `tests/faults.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_recovery.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v03-prove-recovery-under-faults`, and open a PR against `main`.
