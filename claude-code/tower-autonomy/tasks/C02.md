# C02 — Verify card purchases and equipment

**Status:** [ ] Partial — resume
**Domain:** `cards_modules`
**Dependencies:** [C01](./C01.md), [B05](./B05.md), [B07](./B07.md), [F04](./F04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Confirm card pack results and gem deductions, open slots, equip/save presets and enforce the lab-slot-first resource plan; integrate event card-purchase goals.

## Acceptance gate

A purchased pack reconciles copies and gems; a lab-slot reserve is preserved; each preset is read back after applying it.

## Current repository state

- [x] Bounded card-pack buying with a gem reserve floor exists.
- [ ] Still required: pack-result/copy reconciliation, slot opening, preset equipment/readback, and event purchase integration.

## File map

- Inspect/modify: `cards.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `maintenance.py`
- Create when absent: `loadouts.py`
- Focused verification: `tests/test_cards.py`
- Focused verification: `tests/test_loadouts.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/c02-verify-card-purchases-and-equipment` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_cards.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_cards.py tests/test_loadouts.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `cards.py` and `loadouts.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_cards.py tests/test_loadouts.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/c02-verify-card-purchases-and-equipment`, and open a PR against `main`.
