# F03 — Complete Workshop progression

**Status:** [ ] Partial — resume
**Domain:** `farming`
**Dependencies:** [B02](./B02.md), [B04](./B04.md), [B05](./B05.md), [B07](./B07.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Model every standard unlock chain and actual levels/costs, purchase batches and decreasing stats. Replace limited early unlock links with verified account prerequisites and stage-aware exclusions.

## Acceptance gate

An initially locked upgrade becomes discoverable through its prerequisite chain; no premature Wall/Rend or irrelevant unlock is bought; target spending remains bounded.

## Current repository state

- [x] Bounded Workshop shopping and a small early unlock chain exist.
- [ ] Still required: complete stage-aware prerequisites, actual levels/costs, batches, exclusions, and verified unlock discovery.

## File map

- Inspect/modify: `progression.py`
- Inspect/modify: `shopping.py`
- Inspect/modify: `upgrades.py`
- Focused verification: `tests/test_progression.py`
- Focused verification: `tests/test_strategy_shopping.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f03-complete-workshop-progression` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_progression.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_progression.py tests/test_strategy_shopping.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `progression.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_progression.py tests/test_strategy_shopping.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f03-complete-workshop-progression`, and open a PR against `main`.
