# O05 — Provide incident and recovery controls

**Status:** [ ] Partial — resume
**Domain:** `operations`
**Dependencies:** [B06](./B06.md), [F08](./F08.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Show recovery attempts, current blocking evidence, degraded capabilities and safe resume/stop controls; retain machine-readable incident bundles.

## Acceptance gate

A stuck optional subsystem can be quarantined while safe core farming continues; unresolved global state stops spending and gives one actionable incident.

## Current repository state

- [x] Pause/stop controls, errors, events, and unknown-screen views exist.
- [ ] Still required: bounded recovery attempts, incident bundles, capability quarantine, and safe degraded operation.

## File map

- Inspect/modify: `supervisor.py`
- Inspect/modify: `interruptions.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `web/app.py`
- Create when absent: `incidents.py`
- Focused verification: `tests/test_incidents.py`
- Focused verification: `tests/test_recovery.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/o05-provide-incident-and-recovery-controls` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_incidents.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_incidents.py tests/test_recovery.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `supervisor.py` and `incidents.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_incidents.py tests/test_recovery.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/o05-provide-incident-and-recovery-controls`, and open a PR against `main`.
