# B04 — Discover all supported screens and rows

**Status:** [ ] Partial — resume
**Domain:** `foundation`
**Dependencies:** [B02](./B02.md), [B03](./B03.md)
**Dependency gate now:** [x] Ready

## Outcome

Generalize OCR navigation to menus, nested tabs, scroll lists, popups and account readers; use available in-game Battle History/stat exports; validate screen geometry and locale; distinguish locked, unseen, unavailable, maxed and unreadable.

## Acceptance gate

Recorded screens across supported unlock stages resolve stable IDs without fixed row ordering; new tabs do not shift a purchase to a different target.

## Current repository state

- [x] Merged PR #23: supported Workshop and Battle screen discovery baseline.
- [x] Merged PR #26: central fail-closed popup guard.
- [x] Merged PR #27: bounded Battle History report parser.
- [x] Merged PR #28: passive Settings and Stats readers plus account UI.
- [ ] Next slice: an explicit read-only Collect stats transaction that navigates Home → Settings → Stats → Home.
- [ ] Still required afterward: readers and verified targets for the remaining supported menus, tabs, scroll lists, and account stages.

## File map

- Inspect/modify: `account_screens.py`
- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `tower_bot.py`
- Inspect/modify: `runner.py`
- Inspect/modify: `web/app.py`
- Inspect/modify: `web/ui/app/account/page.tsx`
- Create when absent: `account_collection.py`
- Focused verification: `tests/test_account_screens.py`
- Focused verification: `tests/test_runner.py`
- Focused verification: `tests/test_control_api.py`
- Focused verification: `web/ui/app/account/page.test.tsx`

## Execution checklist

- [x] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/b04-discover-all-supported-screens-and-rows` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_account_screens.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_account_screens.py tests/test_runner.py tests/test_control_api.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/account/page.test.tsx` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `account_screens.py` and `account_collection.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_account_screens.py tests/test_runner.py tests/test_control_api.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/account/page.test.tsx` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/b04-discover-all-supported-screens-and-rows`, and open a PR against `main`.
