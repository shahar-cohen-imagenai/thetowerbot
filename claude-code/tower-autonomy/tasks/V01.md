# V01 — Prove subsystem coverage in replay

**Status:** [ ] Partial — resume
**Domain:** `validation`
**Dependencies:** [B08](./B08.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Maintain a scenario matrix per enabled reader/executor, covering unlocks, max levels, missing inputs, currencies, ambiguous outcomes and version changes.

## Acceptance gate

All enabled actions pass their relevant focused fixture/API checks; catalogs, schemas and unexecuted tests do not count as live support.

## Current repository state

- [x] The repository has focused unit tests and several real OCR fixtures.
- [ ] Still required: a machine-readable scenario matrix proving every enabled reader and executor across success and failure states.

## File map

- Inspect/modify: `tests/fixtures`
- Inspect/modify: `transactions.py`
- Create when absent: `tests/capability_matrix.json`
- Focused verification: `tests/test_capability_matrix.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v01-prove-subsystem-coverage-in-replay` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_capability_matrix.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_capability_matrix.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `tests/fixtures` and `tests/capability_matrix.json`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_capability_matrix.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v01-prove-subsystem-coverage-in-replay`, and open a PR against `main`.
