# F09 — Measure outcomes with build context

**Status:** [ ] Partial — resume
**Domain:** `farming`
**Dependencies:** [B03](./B03.md), [B05](./B05.md), [F01](./F01.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Extend run history with verified tier/wave, coins/cells/shards, real elapsed time including visits, death reason, active build and optimizer/account revision; retain missing-data flags.

## Acceptance gate

Rates exclude invalid/zero-duration data; two build variants can be compared using reproducible eligible samples rather than counting taps as successful upgrades.

## Current repository state

- [x] Run records and statistics endpoints exist.
- [ ] Still required: verified rewards/rates, real elapsed time, death reason, build and optimizer revision links, and invalid-sample filtering.

## File map

- Inspect/modify: `runs.py`
- Inspect/modify: `events.py`
- Inspect/modify: `account_state.py`
- Focused verification: `tests/test_runs.py`
- Focused verification: `tests/test_stats_api.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f09-measure-outcomes-with-build-context` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_runs.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_runs.py tests/test_stats_api.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `runs.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_runs.py tests/test_stats_api.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f09-measure-outcomes-with-build-context`, and open a PR against `main`.
