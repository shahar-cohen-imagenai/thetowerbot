# C01 — Read cards, slots and mastery

**Status:** [ ] Partial slice landed; graph task not certified
**Domain:** `cards_modules`
**Dependencies:** [B03](./B03.md), [B04](./B04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Inventory card identities, copies, levels, unlocked/equipped slots, presets, in-run locks and mastery ownership; distinguish passive bonuses from active abilities.

## Acceptance gate

Exact equipped cards and available slots are known before calculating effective stats; missing card observations remain explicit.

## Current repository state

- [ ] No graph-level implementation has been certified complete.
- [x] Landed slice: the Cards page is a recorded reader. `menu_cards.png` (the
  only card capture, 1080x2400, English) is an account that owns **no** card, so
  it proves the page identity, the ACTIVE band ("0 / 1" - zero cards equipped of
  one slot) and one further slot drawn as locked, and it proves nothing about a
  card row. `screen_discovery` gained the `cards` context, three measured
  anchors, `cards.inventory` in `_RECORDED_READERS`, and the recorded OCR
  fixture `tests/fixtures/ocr/menu_cards.json`.
- [x] `cards.py` reads that page: exact equipped set (`()` when the band reads
  zero, `None` when a nonzero count cannot be named), slot counts, the locked
  next slot, and all 62 catalog identities (31 `cards`, 31 `mastery`) carried
  explicitly as `unknown` with `copies`/`level` `None`.
  `effective_stat_inputs()` is the gate a stat consumer calls: it reports
  `equipment_known` and keeps `card_bonus` `None`, never 0. `actions()` always
  returns `()`; this module never taps.
- [x] `account_state.AccountRevision` gained one field, `cards`, persisted and
  restored through `AccountRepository`; `None` means the page was never read.
- [ ] Still unread, and now named in `capabilities()['unsupported']` with
  owners: card identity rows, copies and levels; card mastery ownership;
  presets; in-run locks (`B08` - no capture of a stocked collection exists);
  the passive-bonus / active-ability split (`B02` - registry v1 files all 31
  cards under the single kind `card`, so no identity carries it); slot and card
  purchase actions (`C02`).

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `screen_discovery.py`
- Create when absent: `cards.py`
- Focused verification: `tests/test_cards.py`

## Execution checklist

- [x] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [x] Create branch `codex/c01-read-cards-slots-and-mastery` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [x] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [x] Add failing tests in `tests/test_cards.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [x] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_cards.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [x] Implement the smallest complete change in `account_state.py` and `cards.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [x] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_cards.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/c01-read-cards-slots-and-mastery`, and open a PR against `main`.
