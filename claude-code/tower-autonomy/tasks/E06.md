# E06 — Check proposals before execution

**Status:** [ ] Not started
**Domain:** `effective_paths`
**Dependencies:** [E05](./E05.md), [B05](./B05.md), [B08](./B08.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Run feasibility and affordability checks, simulate the planned stat/resource change, assert sync and build invariants, and compare against workbook fixture outputs in shadow mode.

## Acceptance gate

Invalid levels, wrong units, unaffordable bundles, stale evidence and broken synchronization are rejected in replay without affecting the device.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `planner.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `tests/fixtures`
- Create when absent: `proposal_checks.py`
- Focused verification: `tests/test_proposal_checks.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/e06-check-proposals-before-execution` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_proposal_checks.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_proposal_checks.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `planner.py` and `proposal_checks.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_proposal_checks.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/e06-check-proposals-before-execution`, and open a PR against `main`.
