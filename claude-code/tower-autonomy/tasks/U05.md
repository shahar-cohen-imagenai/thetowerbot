# U05 — Manage event bots and Bot+

**Status:** [ ] Not started
**Domain:** `ultimate_weapons`
**Dependencies:** [B03](./B03.md), [B04](./B04.md), [B05](./B05.md), [B07](./B07.md), [U03](./U03.md), [T02](./T02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Model each current event bot, unlock/medal levels, cooldown/range/duration/bonus, labs and toggles; cover Bot Bot, Bot+ and Synchronicity movement-slot assignment when eligible; movement geometry is distinct from cooldown synchronization.

## Acceptance gate

A bot upgrade reconciles medals and levels while preserving protected sync and event purchases; a glossary count cannot hide a current bot.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `ultimate_weapons.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `currencies.py`
- Create when absent: `event_bots.py`
- Focused verification: `tests/test_event_bots.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/u05-manage-event-bots-and-bot` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_event_bots.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_event_bots.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `ultimate_weapons.py` and `event_bots.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_event_bots.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/u05-manage-event-bots-and-bot`, and open a PR against `main`.
