# A01 — Buy Workshop Enhancements

**Status:** [ ] Not started
**Domain:** `advanced`
**Dependencies:** [F03](./F03.md), [B07](./B07.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read enhancement unlocks, levels, coin costs and category progression; execute EP enhancement recommendations independently of ordinary Workshop stats.

## Acceptance gate

An enhancement purchase updates the matching calculator multiplier once and confirms any newly unlocked enhancement options.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `progression.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `planner.py`
- Create when absent: `enhancements.py`
- Focused verification: `tests/test_enhancements.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a01-buy-workshop-enhancements` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_enhancements.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_enhancements.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `progression.py` and `enhancements.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_enhancements.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a01-buy-workshop-enhancements`, and open a PR against `main`.
