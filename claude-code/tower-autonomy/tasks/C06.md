# C06 — Plan card mastery investments

**Status:** [ ] Not started
**Domain:** `cards_modules`
**Dependencies:** [C01](./C01.md), [C02](./C02.md), [L02](./L02.md), [E05](./E05.md), [U02](./U02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Model mastery unlock conditions, stone unlocks, coin/lab follow-ups and active/persistent effects; compare expensive mastery paths with competing UWs and research.

## Acceptance gate

A mastery plan checks its full unlock and follow-up costs, updates effective stats, and never spends the stone reserve twice.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `cards.py`
- Inspect/modify: `loadouts.py`
- Inspect/modify: `planner.py`
- Inspect/modify: `labs.py`
- Create when absent: `mastery.py`
- Focused verification: `tests/test_mastery.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/c06-plan-card-mastery-investments` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_mastery.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_mastery.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `cards.py` and `mastery.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_mastery.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/c06-plan-card-mastery-investments`, and open a PR against `main`.
