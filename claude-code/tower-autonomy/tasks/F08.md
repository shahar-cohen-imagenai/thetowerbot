# F08 — Handle rewards and blocking overlays

**Status:** [ ] Not started
**Domain:** `farming`
**Dependencies:** [B04](./B04.md), [B05](./B05.md), [B06](./B06.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Classify daily login, gems, reward claims, result screens, known ad flows, disconnect notices and known dismissible popups. Track reward availability, completion and timeout.

## Acceptance gate

Known interruptions resume the prior objective with verified rewards; an unrecognized overlay triggers bounded recovery instead of blind clicks or endless scanning.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `tower_bot.py`
- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `supervisor.py`
- Create when absent: `interruptions.py`
- Focused verification: `tests/test_interruptions.py`
- Focused verification: `tests/test_recovery.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f08-handle-rewards-and-blocking-overlays` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_interruptions.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_interruptions.py tests/test_recovery.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `tower_bot.py` and `interruptions.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_interruptions.py tests/test_recovery.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f08-handle-rewards-and-blocking-overlays`, and open a PR against `main`.
