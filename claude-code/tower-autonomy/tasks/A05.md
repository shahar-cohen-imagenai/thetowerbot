# A05 — Manage Vault trees and keys

**Status:** [ ] Not started
**Domain:** `advanced`
**Dependencies:** [B03](./B03.md), [B04](./B04.md), [B05](./B05.md), [B07](./B07.md), [E05](./E05.md), [T03](./T03.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read the current tree catalog, unlock prerequisites and key economy; implement tree purchases/respec policy and integrate the native EP key path where supported.

## Acceptance gate

A tree node purchase confirms prerequisite, key cost and resulting bonus; old two-tree documentation cannot truncate the current catalog.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `planner.py`
- Create when absent: `vault.py`
- Focused verification: `tests/test_vault.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a05-manage-vault-trees-and-keys` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_vault.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_vault.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `account_state.py` and `vault.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_vault.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a05-manage-vault-trees-and-keys`, and open a PR against `main`.
