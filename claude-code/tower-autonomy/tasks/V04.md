# V04 — Certify overnight farming

**Status:** [ ] Not started
**Domain:** `validation`
**Dependencies:** [F04](./F04.md), [F05](./F05.md), [F06](./F06.md), [F07](./F07.md), [F08](./F08.md), [L02](./L02.md), [C02](./C02.md), [V03](./V03.md), [O05](./O05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Run a controlled 24-hour acceptance session with available early-game systems, a fixed policy and recorded baseline; inspect intervention and action outcomes.

## Acceptance gate

Zero routine manual taps; every purchase reconciled; no indefinitely blocked known screen; all due supported maintenance serviced; resource reserves preserved.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `autopilot.py`
- Inspect/modify: `maintenance.py`
- Inspect/modify: `runs.py`
- Inspect/modify: `incidents.py`
- Create when absent: `qualification/overnight.py`
- Focused verification: `tests/test_overnight_qualification.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v04-certify-overnight-farming` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_overnight_qualification.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_overnight_qualification.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `autopilot.py` and `qualification/overnight.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_overnight_qualification.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v04-certify-overnight-farming`, and open a PR against `main`.
