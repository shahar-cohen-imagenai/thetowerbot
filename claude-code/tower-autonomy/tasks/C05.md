# C05 — Optimize module loadouts and subeffects

**Status:** [ ] Not started
**Domain:** `cards_modules`
**Dependencies:** [C03](./C03.md), [C04](./C04.md), [B07](./B07.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Rank equip/assist combinations per build, reroll with desired effects, bans, native Auto Roll and locked slots, enforce shard budgets and exact stop conditions; preserve existing useful subeffects.

## Acceptance gate

Reroll stops on desired outcomes or budget exhaustion; replacing a module invalidates affected optimizer inputs before another recommendation executes.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `modules.py`
- Inspect/modify: `loadouts.py`
- Inspect/modify: `planner.py`
- Focused verification: `tests/test_module_optimizer.py`
- Focused verification: `tests/test_loadouts.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/c05-optimize-module-loadouts-and-subeffects` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_module_optimizer.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_module_optimizer.py tests/test_loadouts.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `modules.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_module_optimizer.py tests/test_loadouts.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/c05-optimize-module-loadouts-and-subeffects`, and open a PR against `main`.
