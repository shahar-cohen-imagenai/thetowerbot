# B08 — Build representative replay fixtures

**Status:** [ ] Partial — resume
**Domain:** `foundation`
**Dependencies:** [B02](./B02.md), [B04](./B04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Extend OCR fixtures with before/after action sequences and account snapshots for each supported subsystem, ambiguous reads, expensive confirmations, overlays and late-game layouts.

## Acceptance gate

Each enabled capability has positive, unavailable and ambiguous-state examples plus a reproducible focused verification command; unsupported capabilities remain disabled.

## Current repository state

- [x] Focused OCR, screen, overlay, and account-reader fixtures exist.
- [ ] Still required: before/after action sequences and positive, unavailable, and ambiguous coverage for every enabled capability.

## File map

- Inspect/modify: `tests/fixtures`
- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `account_screens.py`
- Create when absent: `tests/fixtures/account`
- Focused verification: `tests/test_fixtures.py`
- Focused verification: `tests/test_screen_discovery.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/b08-build-representative-replay-fixtures` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_fixtures.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_fixtures.py tests/test_screen_discovery.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `tests/fixtures` and `tests/fixtures/account`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_fixtures.py tests/test_screen_discovery.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/b08-build-representative-replay-fixtures`, and open a PR against `main`.
