# L03 — Schedule lab acceleration

**Status:** [ ] Not started
**Domain:** `labs`
**Dependencies:** [L02](./L02.md), [B07](./B07.md), [F09](./F09.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Allocate cells to supported speed-up tiers/durations and optional gem rushing under explicit saved limits; model real completion time, queue occupancy and next boost expiry.

## Acceptance gate

Boosts stay within projected cell income/reserve, expired boosts are renewed when eligible, and unavailable acceleration is skipped without blocking research.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `labs.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `runs.py`
- Focused verification: `tests/test_lab_acceleration.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/l03-schedule-lab-acceleration` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_lab_acceleration.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_lab_acceleration.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `labs.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_lab_acceleration.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/l03-schedule-lab-acceleration`, and open a PR against `main`.
