# E04 — Normalize all native recommendation types

**Status:** [ ] Partial — resume
**Domain:** `effective_paths`
**Dependencies:** [E03](./E03.md), [B02](./B02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Extend the manual advisor import to native eHP/eRegen/eDamage/eEcon variants, coin/stone/key and discount outputs with levels, units, gain, cost, duration, uncertainty and executor capability.

## Acceptance gate

Each result is executable, blocked for a named prerequisite, or advisory with a reason; standard Workshop planning is not falsely presented as a native EP path.

## Current repository state

- [x] The advisor import path normalizes a limited set of structured recommendations.
- [ ] Still required: all native workbook output families, strict units/ranges, provenance, and unsupported-type disabling.

## File map

- Inspect/modify: `effective_paths/connector.py`
- Inspect/modify: `concepts.py`
- Create when absent: `effective_paths/recommendations.py`
- Focused verification: `tests/test_effective_paths_recommendations.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/e04-normalize-all-native-recommendation-types` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_effective_paths_recommendations.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_recommendations.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `effective_paths/connector.py` and `effective_paths/recommendations.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_recommendations.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/e04-normalize-all-native-recommendation-types`, and open a PR against `main`.
