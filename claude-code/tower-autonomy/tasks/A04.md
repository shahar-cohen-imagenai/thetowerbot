# A04 — Control targeting, range and positioning

**Status:** [ ] Not started
**Domain:** `advanced`
**Dependencies:** [B04](./B04.md), [B05](./B05.md), [F01](./F01.md), [F06](./F06.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Execute target priority, orb adjustments, range/shockwave sliders, UW toggles and other supported in-run controls; account for enemies, elites and battle conditions.

## Acceptance gate

Controls are applied and read back for the current mode; locked or unsupported controls are skipped with an explicit capability reason.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `combat_context.py`
- Inspect/modify: `perks.py`
- Create when absent: `targeting.py`
- Focused verification: `tests/test_targeting.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a04-control-targeting-range-and-positioning` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_targeting.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_targeting.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `screen_discovery.py` and `targeting.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_targeting.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a04-control-targeting-range-and-positioning`, and open a PR against `main`.
