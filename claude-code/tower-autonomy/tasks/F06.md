# F06 — Choose and configure perks

**Status:** [ ] Not started
**Domain:** `farming`
**Dependencies:** [B04](./B04.md), [B05](./B05.md), [F01](./F01.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read offered normal/UW/tradeoff perks, current stacks and perk settings; implement build/purpose-specific picks, bans, priorities and unlocks for automatic perk facilities.

## Acceptance gate

Handle a blocking perk choice and resume the run; reject a harmful tradeoff, deliberately defer a phase-sensitive perk, and restore native auto-pick settings after the run; record why.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `policy.py`
- Inspect/modify: `tower_bot.py`
- Inspect/modify: `screen_discovery.py`
- Create when absent: `perks.py`
- Focused verification: `tests/test_perks.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f06-choose-and-configure-perks` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_perks.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_perks.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `policy.py` and `perks.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_perks.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f06-choose-and-configure-perks`, and open a PR against `main`.
