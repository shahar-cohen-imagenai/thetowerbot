# B07 — Allocate currencies and reserves

**Status:** [ ] Partial — resume
**Domain:** `foundation`
**Dependencies:** [B03](./B03.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Separate run cash from permanent coins, gems, stones, medals, cells, keys, shards, tickets, Bits and other versioned resources. Reserve across competing plans and timers, not just per shopping visit.

## Acceptance gate

Two queued actions cannot reserve the same funds; lab-slot savings and UW synchronization savings survive lower-priority purchases and process restarts.

## Current repository state

- [x] Shopping profiles expose budgets and simple coin/gem reserve floors.
- [ ] Still required: durable multi-currency commitments shared across competing plans and timers.

## File map

- Inspect/modify: `policy.py`
- Inspect/modify: `account_state.py`
- Inspect/modify: `shopping.py`
- Create when absent: `currencies.py`
- Focused verification: `tests/test_currencies.py`
- Focused verification: `tests/test_autopilot_policy.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/b07-allocate-currencies-and-reserves` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_currencies.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_currencies.py tests/test_autopilot_policy.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `policy.py` and `currencies.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_currencies.py tests/test_autopilot_policy.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/b07-allocate-currencies-and-reserves`, and open a PR against `main`.
