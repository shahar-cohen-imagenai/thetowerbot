# T06 — Automate Dissonance and Echo progression

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [A03](./A03.md), [A04](./A04.md), [U04](./U04.md), [A05](./A05.md), [A06](./A06.md), [T04](./T04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read Dissonance unlocks/run constraints, Echo and build-specific requirements; plan specialized attempts, respec/loadout restoration and permanent reward effects.

## Acceptance gate

Complete a supported Dissonant objective with verified setup, reward and return to farming; no late-game mode is marked supported from its name alone.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `builds.py`
- Inspect/modify: `targeting.py`
- Inspect/modify: `ultimate_weapons.py`
- Inspect/modify: `vault.py`
- Inspect/modify: `guardian.py`
- Inspect/modify: `rewards.py`
- Create when absent: `dissonance.py`
- Focused verification: `tests/test_dissonance.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t06-automate-dissonance-and-echo-progression` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_dissonance.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_dissonance.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `builds.py` and `dissonance.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_dissonance.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t06-automate-dissonance-and-echo-progression`, and open a PR against `main`.
