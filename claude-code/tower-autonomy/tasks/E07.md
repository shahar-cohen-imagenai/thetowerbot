# E07 — Close the observe–optimize–act loop

**Status:** [ ] Not started
**Domain:** `effective_paths`
**Dependencies:** [E06](./E06.md), [F04](./F04.md), [L04](./L04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Promote eligible plans under saved authority, dispatch to available domain executors, verify the change, refresh inputs and replan; continue safe farming when an optional path is unsupported.

## Acceptance gate

One supported native recommendation completes from account observation through confirmed in-game change and a new calculation without import, stage or Save clicks.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `planner.py`
- Inspect/modify: `proposal_checks.py`
- Inspect/modify: `maintenance.py`
- Focused verification: `tests/test_optimizer_loop.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/e07-close-the-observe-optimize-act-loop` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_optimizer_loop.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_optimizer_loop.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `planner.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_optimizer_loop.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/e07-close-the-observe-optimize-act-loop`, and open a PR against `main`.
