# F02 — Complete battle upgrade execution

**Status:** [ ] Partial — resume
**Domain:** `farming`
**Dependencies:** [F01](./F01.md), [B05](./B05.md), [B07](./B07.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Finish Attack/Defense/Utility traversal, target semantics, buy mode, reserves and maxed rows; make Turtle/Health policy prerequisites explicit and support revisiting affordable priorities.

## Acceptance gate

A recorded multi-tab run buys the intended semantic upgrade once, handles free-upgrade interference, respects cash reserve and reaches its configured target.

## Current repository state

- [x] A semantic OCR shopping engine, battle presets, tab navigation, targets, and bounded visits exist.
- [ ] Still required: complete Attack/Defense/Utility execution with verified reserves, maxed rows, free-upgrade interference, and target completion.

## File map

- Inspect/modify: `shopping.py`
- Inspect/modify: `strategy.py`
- Inspect/modify: `tower_bot.py`
- Focused verification: `tests/test_shopping_loop.py`
- Focused verification: `tests/test_shopping_autopilot.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f02-complete-battle-upgrade-execution` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_shopping_loop.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_shopping_loop.py tests/test_shopping_autopilot.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `shopping.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_shopping_loop.py tests/test_shopping_autopilot.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f02-complete-battle-upgrade-execution`, and open a PR against `main`.
