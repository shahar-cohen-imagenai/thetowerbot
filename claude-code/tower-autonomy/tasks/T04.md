# T04 — Track milestones, relics and cosmetics

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [F05](./F05.md), [T01](./T01.md), [T02](./T02.md), [B03](./B03.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read and claim tier milestones and unlocks; inventory relics, skins/backgrounds/songs and effective bonuses; model eligible free/owned pass rewards separately.

## Acceptance gate

Claimed rewards and passive bonuses reconcile once; future model calculations include owned bonuses even when a cosmetic is not equipped.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `tier_policy.py`
- Inspect/modify: `missions.py`
- Inspect/modify: `rewards.py`
- Inspect/modify: `account_state.py`
- Inspect/modify: `transactions.py`
- Focused verification: `tests/test_rewards.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t04-track-milestones-relics-and-cosmetics` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_rewards.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_rewards.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `tier_policy.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_rewards.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t04-track-milestones-relics-and-cosmetics`, and open a PR against `main`.
