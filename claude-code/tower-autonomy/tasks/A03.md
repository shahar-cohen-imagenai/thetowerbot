# A03 — Manage build transitions and respecs

**Status:** [ ] Not started
**Domain:** `advanced`
**Dependencies:** [F02](./F02.md), [F03](./F03.md), [F06](./F06.md), [F07](./F07.md), [C05](./C05.md), [U03](./U03.md), [E05](./E05.md), [A04](./A04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Model Turtle, Blender/eHP, hybrid, Devo/orb-devo/orbless and glass-cannon eligibility with account-specific goals; plan reversible loadout changes and controlled Workshop respecs.

## Acceptance gate

A chosen transition has a verified precondition checklist and ordered restore plan; free-upgrade, range and sync settings match the intended build before farming resumes.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `builds.py`
- Inspect/modify: `run_loadouts.py`
- Inspect/modify: `planner.py`
- Focused verification: `tests/test_build_transitions.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a03-manage-build-transitions-and-respecs` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_build_transitions.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_build_transitions.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `builds.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_build_transitions.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a03-manage-build-transitions-and-respecs`, and open a PR against `main`.
