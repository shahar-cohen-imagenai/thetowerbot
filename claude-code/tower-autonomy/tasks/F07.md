# F07 — Manage run loadouts and active skills

**Status:** [ ] Not started
**Domain:** `farming`
**Dependencies:** [F01](./F01.md), [C01](./C01.md), [C02](./C02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Select farming, milestone, event and tournament card sets; track swap restrictions and active-card cooldown/usage; coordinate UW and targeting settings when their executors become available.

## Acceptance gate

Loadout is confirmed before the run; boss/Fleet and other locked in-run cards are not swapped; active abilities fire at configured conditions and restore the farming preset afterward.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `policy.py`
- Inspect/modify: `tower_bot.py`
- Inspect/modify: `strategy.py`
- Create when absent: `run_loadouts.py`
- Focused verification: `tests/test_run_loadouts.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f07-manage-run-loadouts-and-active-skills` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_run_loadouts.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_run_loadouts.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `policy.py` and `run_loadouts.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_run_loadouts.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f07-manage-run-loadouts-and-active-skills`, and open a PR against `main`.
