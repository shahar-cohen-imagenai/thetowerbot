# B06 — Recover the device and bot lifecycle

**Status:** [ ] Partial — resume
**Domain:** `foundation`
**Dependencies:** [B01](./B01.md), [B05](./B05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Use a single device lease and durable supervisor for ADB reconnect, game resume/relaunch, transient failures, bounded retry/backoff and crash recovery; preserve the verified account identity.

## Acceptance gate

Inject disconnect, stale frame, online-required action and process restart; recover the correct device and pending action or enter an explicit bounded blocked state without uncontrolled tapping.

## Current repository state

- [x] ADB capture and bot start/stop/pause lifecycle controls exist.
- [ ] Still required: a single device lease, durable supervisor, bounded reconnect/relaunch, and pending-action recovery.

## File map

- Inspect/modify: `device.py`
- Inspect/modify: `runner.py`
- Inspect/modify: `tower_bot.py`
- Create when absent: `supervisor.py`
- Focused verification: `tests/test_supervisor.py`
- Focused verification: `tests/test_device.py`
- Focused verification: `tests/test_runner.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/b06-recover-the-device-and-bot-lifecycle` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_supervisor.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_supervisor.py tests/test_device.py tests/test_runner.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `device.py` and `supervisor.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_supervisor.py tests/test_device.py tests/test_runner.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/b06-recover-the-device-and-bot-lifecycle`, and open a PR against `main`.
