# B05 — Journal and verify every transaction

**Status:** [x] Complete — acceptance gate verified and merged to `main`
**Domain:** `foundation`
**Dependencies:** [B03](./B03.md), [B04](./B04.md)
**Dependency gate now:** [x] B03 and B04 are completed in the manifest

## Outcome

Introduce durable intent, preconditions, one device action, confirmation and reconciliation. Correlate wallet, level/inventory and UI acknowledgement; separate free upgrades and passive income from purchased effects.

## Acceptance gate

A crash after a successful tap does not buy twice; a free upgrade does not falsely confirm a cash purchase; card opening is not logged as bought without evidence.

## Current repository state

- [x] The existing ledger and shopping loop record bounded purchase-related events.
- [x] Landed slice: `transactions.py` holds the durable half of every spend.
  A row in the new `transactions` table is written BEFORE the device action
  and resolved after it, so the window between a tap and its confirmation -
  previously recorded nowhere on disk - now outlives the process. `Stage`
  keeps `INTENDED` (nothing was spent) apart from `ACTED` (one action went
  out, outcome unknown). The journal owns a path and opens a short
  connection per operation, the way `AccountRepository` does, so a session
  built on one thread is usable from the scan loop's.
- [x] Landed slice: one transaction owns exactly one device action
  (`ActionAlreadyTaken`), and no attempt may be opened while an earlier one
  is unanswered (`TransactionInFlight`).
- [x] Landed slice: `transactions.judge` is the whole confirmation rule,
  pure and database-free. `BOUGHT` requires the wallet to have fallen;
  `FREE` is a changed item that cost nothing; `REFUTED` needs both an
  unchanged item and an unmoved wallet; everything else is `UNPROVEN`.
  `spent` stays three-valued - a proven amount, 0 for provably nothing,
  `None` for moved-by-an-unknown-amount - so income landing mid-purchase
  leaves the amount unknown rather than being asserted as the read price.
- [x] Landed slice: both buy paths in `shopping.py` are journalled. A
  workshop row and a card batch each write an intent before tapping, mark
  the action, and resolve against the evidence that follows. A row left
  open by a dead process owns the next scan until fresh evidence proves its
  outcome; ambiguous evidence keeps it blocked. An orderly `reset()` or
  `_end_visit()` resolves only a pending attempt owned by that process.
  `begin()` never erases the dead process's open row.
- [x] Landed slice: card opening now requires evidence. `PendingCard` holds
  the tap until the gem balance falls by exactly the price; `Purchased`
  with category `CARDS` is no longer published on the same frame as the
  tap. An inconclusive card acknowledgement aborts the visit, as an
  inconclusive workshop one already did.
- [x] Landed slice: ledger debits, `_spent`, `_coin_spent`, and visit budgets
  use the journal verdict and proven amount. A read price never substitutes
  for an unknown spend; free upgrades never create a cash debit.
- [x] Locally verified restart reconciliation: an orphaned `ACTED` row owns
  the scan before any speed, claim, navigation, or shopping action, including
  after runner reset and when a different page is visible. Two fresh matching
  readings must prove the relevant currency debit and item effect. Workshop
  proof uses the persisted pre-action identity/value and fresh account facts;
  card proof requires the exact gem debit on the Cards page.
- [x] Missing, stale, mismatched, unreadable, or ambiguous evidence retains an
  explicit `UNPROVEN` outcome on the open row. Reset, visit termination, and
  reader failures cannot erase that block or trigger an abort tap. Legacy
  Workshop rows lacking a usable baseline remain blocked.
- [x] A proven recovery writes the journal verdict and ledger movement in one
  SQLite transaction. Delivery of its telemetry event is optional for
  durability and cannot duplicate the debit. Ledger writers refresh their
  cached balances after recovery writes.
- [x] Durable recovery receipts restore spending and purchase counts after
  another crash. Recovery finishes the interrupted visit before permitting
  further shopping; its receipt is released only when that visit ends.
  Workshop account observations retain their normal two-reading verification.
- [x] Local verification: the focused command below passed **173 tests**.
  `npm run build --prefix web/ui` passed; both build-manifest hashes match
  current sources. Backend hash:
  `132cf07cc1a74bff83ea44ce3b578895ad14b3368a06e44ab3a639e6b36a1992`.
  Independent code review found no remaining important issues. No live
  device actions or account queries were used.
- [x] The verified implementation is merged to `main`; the graph task is complete.

## Reproducible verification

```
/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest \
  -p no:allure_pytest tests/test_transactions.py tests/test_ledger.py \
  tests/test_shopping.py -q
```

## File map

- Inspect/modify: `ledger.py`
- Inspect/modify: `tower_bot.py`
- Inspect/modify: `account_state.py`
- Create when absent: `transactions.py`
- Focused verification: `tests/test_transactions.py`
- Focused verification: `tests/test_ledger.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/b05-journal-and-verify-every-transaction` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_transactions.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_transactions.py tests/test_ledger.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `ledger.py` and `transactions.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_transactions.py tests/test_ledger.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/b05-journal-and-verify-every-transaction`, and open a PR against `main`.
