# T05 — Support guild and seasonal systems

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [T01](./T01.md), [T02](./T02.md), [F07](./F07.md), [B07](./B07.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Version guild objectives, weekly contributions, seasonal shop stock, token conversion and Bits; add readers, attainable-goal policy and actions for the supported account/version.

## Acceptance gate

A supported activity has known schedule, resource and reward semantics; social/account setup dependencies are recorded and do not block ordinary solo farming.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `missions.py`
- Inspect/modify: `rewards.py`
- Inspect/modify: `run_loadouts.py`
- Inspect/modify: `currencies.py`
- Create when absent: `guilds.py`
- Focused verification: `tests/test_guilds.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t05-support-guild-and-seasonal-systems` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_guilds.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_guilds.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `missions.py` and `guilds.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_guilds.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t05-support-guild-and-seasonal-systems`, and open a PR against `main`.
