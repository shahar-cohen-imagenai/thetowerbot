# E02 — Populate calculator inputs from account state

**Status:** [ ] Not started
**Domain:** `effective_paths`
**Dependencies:** [E01](./E01.md), [B03](./B03.md), [L01](./L01.md), [C01](./C01.md), [C03](./C03.md), [U01](./U01.md), [F09](./F09.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Map readers to IDS and direct EP inputs, preserving base levels versus run-max levels, raw UWs versus lab/subeffect modifiers, loadout, perks, modules, economy outcomes and estimated assumptions. Add Guardian/Vault/Dissonance inputs as those readers become available; each path declares its required capabilities.

## Acceptance gate

Every required field has provenance or an explicit assumption; public template/example values cannot be mistaken for this account; stale critical fields block only dependent paths.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `effective_paths_contract.py`
- Inspect/modify: `runs.py`
- Create when absent: `effective_paths/inputs.py`
- Focused verification: `tests/test_effective_paths_inputs.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/e02-populate-calculator-inputs-from-account-state` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_effective_paths_inputs.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_inputs.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `account_state.py` and `effective_paths/inputs.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_inputs.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/e02-populate-calculator-inputs-from-account-state`, and open a PR against `main`.
