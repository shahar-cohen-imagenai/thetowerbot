# O04 — Expose schedules and progression results

**Status:** [ ] Partial — resume
**Domain:** `operations`
**Dependencies:** [F09](./F09.md), [L03](./L03.md), [T01](./T01.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Show research queue, currency commitments, event/tournament deadlines, next maintenance visit, progression metrics and goal completion; keep automation schedule independent of the dashboard tab.

## Acceptance gate

Closing the browser does not stop scheduling; measured results link to the exact build and decision history.

## Current repository state

- [x] Run, statistics, and account views expose historical outcomes.
- [ ] Still required: durable queues, commitments, deadlines, maintenance timing, and goal progress independent of browser state.

## File map

- Inspect/modify: `runs.py`
- Inspect/modify: `labs.py`
- Inspect/modify: `missions.py`
- Inspect/modify: `web/app.py`
- Create when absent: `web/ui/app/progression/schedule.tsx`
- Focused verification: `tests/test_schedule_api.py`
- Focused verification: `web/ui/app/progression/schedule.test.tsx`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/o04-expose-schedules-and-progression-results` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_schedule_api.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_schedule_api.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/progression/schedule.test.tsx` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `runs.py` and `web/ui/app/progression/schedule.tsx`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_schedule_api.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/progression/schedule.test.tsx` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/o04-expose-schedules-and-progression-results`, and open a PR against `main`.
