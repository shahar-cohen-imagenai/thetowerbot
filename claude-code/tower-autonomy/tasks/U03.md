# U03 — Maintain UW and bot synchronization

**Status:** [ ] Not started
**Domain:** `ultimate_weapons`
**Dependencies:** [U01](./U01.md), [F01](./F01.md), [B02](./B02.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Implement the read-only protected-sync validator before stone execution; model base cooldowns, durations, overlap, partial versus full sync, UW timing toggles and module/perk modifiers; preserve account-specific GT/BH/DW/Golden Bot constraints.

## Acceptance gate

A proposed cooldown change cannot silently break a protected overlap; actual observed cycles and model assumptions are reported separately.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `ultimate_weapons.py`
- Inspect/modify: `combat_context.py`
- Inspect/modify: `concepts.py`
- Create when absent: `synchronization.py`
- Focused verification: `tests/test_synchronization.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/u03-maintain-uw-and-bot-synchronization` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_synchronization.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_synchronization.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `ultimate_weapons.py` and `synchronization.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_synchronization.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/u03-maintain-uw-and-bot-synchronization`, and open a PR against `main`.
