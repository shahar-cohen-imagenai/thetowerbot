# F05 — Choose tiers and run objectives

**Status:** [ ] Partial — resume
**Domain:** `farming`
**Dependencies:** [F01](./F01.md), [F04](./F04.md), [F09](./F09.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Execute tier selection/unlocks and farm versus milestone objectives. Compare coins/hour and cells/hour including downtime; explore enough eligible tiers and record confidence/build context.

## Acceptance gate

Select and visually confirm the requested tier; do not infer a best tier from Tier 1 alone or mix short milestone attempts with ordinary farming samples.

## Current repository state

- [x] Run history exposes farm-purpose samples and coins-per-hour comparisons.
- [ ] Still required: verified tier selection/unlocks, objective choice, exploration policy, downtime-aware rates, and confidence by build.

## File map

- Inspect/modify: `autopilot.py`
- Inspect/modify: `runs.py`
- Inspect/modify: `navigate.py`
- Create when absent: `tier_policy.py`
- Focused verification: `tests/test_tier_policy.py`
- Focused verification: `tests/test_runs.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f05-choose-tiers-and-run-objectives` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_tier_policy.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_tier_policy.py tests/test_runs.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `autopilot.py` and `tier_policy.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_tier_policy.py tests/test_runs.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f05-choose-tiers-and-run-objectives`, and open a PR against `main`.
