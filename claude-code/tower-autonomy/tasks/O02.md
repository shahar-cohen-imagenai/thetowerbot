# O02 — Configure goals and standing authority

**Status:** [ ] Partial — resume
**Domain:** `operations`
**Dependencies:** [B07](./B07.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Provide saved farming/progression priorities, reserves, forbidden upgrades, sync constraints, allowed currencies and mode schedules; make no-routine-approval mode explicit.

## Acceptance gate

A saved policy authorizes supported routine actions without repeated confirmations while unsupported/forbidden actions cannot be armed accidentally.

## Current repository state

- [x] Strategy and autopilot settings expose basic profiles, budgets, and enablement controls.
- [ ] Still required: durable goals, reserves, forbidden actions, schedules, allowed currencies, and explicit standing authority.

## File map

- Inspect/modify: `policy.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `planner.py`
- Create when absent: `web/ui/app/progression/page.tsx`
- Focused verification: `tests/test_policy_authority.py`
- Focused verification: `web/ui/app/progression/page.test.tsx`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/o02-configure-goals-and-standing-authority` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_policy_authority.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_policy_authority.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/progression/page.test.tsx` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `policy.py` and `web/ui/app/progression/page.tsx`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_policy_authority.py -q`; then run `npm test --prefix web/ui -- --run web/ui/app/progression/page.test.tsx` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/o02-configure-goals-and-standing-authority`, and open a PR against `main`.
