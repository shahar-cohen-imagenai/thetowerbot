# T03 — Participate in tournaments

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [F05](./F05.md), [F07](./F07.md), [F09](./F09.md), [T01](./T01.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read tournament schedule/league/tickets/rewards and battle conditions including Overheat and separate Enemy Skip Decay/Multiply/Subtract effects; choose a competitive loadout and time window, enter, run, claim, and restore farming.

## Acceptance gate

One tournament cycle respects tickets, applies the correct conditions, records result and returns to farming; tournament runs do not distort farming comparisons.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `tier_policy.py`
- Inspect/modify: `run_loadouts.py`
- Inspect/modify: `runs.py`
- Inspect/modify: `missions.py`
- Inspect/modify: `planner.py`
- Create when absent: `tournaments.py`
- Focused verification: `tests/test_tournaments.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t03-participate-in-tournaments` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_tournaments.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_tournaments.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `tier_policy.py` and `tournaments.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_tournaments.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t03-participate-in-tournaments`, and open a PR against `main`.
