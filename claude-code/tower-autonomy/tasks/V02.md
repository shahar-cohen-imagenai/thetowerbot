# V02 — Prove calculator and decision parity

**Status:** [ ] Not started
**Domain:** `validation`
**Dependencies:** [E06](./E06.md), [V01](./V01.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Run representative account fixtures through native EP and the connector; verify ranges, units, output order and tolerances; shadow proposed decisions against actual account observations.

## Acceptance gate

Every enabled native path has version-pinned golden cases; any mismatch disables affected execution until resolved.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `effective_paths/connector.py`
- Inspect/modify: `proposal_checks.py`
- Inspect/modify: `tests/fixtures/effective_paths`
- Focused verification: `tests/test_effective_paths_parity.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v02-prove-calculator-and-decision-parity` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_effective_paths_parity.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_parity.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `effective_paths/connector.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_effective_paths_parity.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v02-prove-calculator-and-decision-parity`, and open a PR against `main`.
