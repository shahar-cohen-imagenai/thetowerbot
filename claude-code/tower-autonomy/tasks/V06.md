# V06 — Maintain version compatibility

**Status:** [ ] Not started
**Domain:** `validation`
**Dependencies:** [B01](./B01.md), [B02](./B02.md), [E01](./E01.md), [V01](./V01.md), [O05](./O05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Detect game, wiki, glossary and workbook changes; diff concepts and fixtures; quarantine incompatible paths and roll out updated adapters after focused qualification.

## Acceptance gate

A changed label, new system or workbook schema cannot silently reroute spending; unaffected verified farming continues when safe.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `runtime_identity.py`
- Inspect/modify: `concepts.py`
- Inspect/modify: `effective_paths_contract.py`
- Inspect/modify: `tests/capability_matrix.json`
- Inspect/modify: `incidents.py`
- Create when absent: `compatibility.py`
- Focused verification: `tests/test_compatibility.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/v06-maintain-version-compatibility` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_compatibility.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_compatibility.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `runtime_identity.py` and `compatibility.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_compatibility.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/v06-maintain-version-compatibility`, and open a PR against `main`.
