# A06 — Manage Guardian chips and materials

**Status:** [ ] Not started
**Domain:** `advanced`
**Dependencies:** [B03](./B03.md), [B04](./B04.md), [B05](./B05.md), [B07](./B07.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read Guardian unlocks and current chip catalog including Scout, levels, cooldowns, materials and effects; support IDS input mapping, purchases and run usage.

## Acceptance gate

An eligible chip action reconciles materials and updates affected economy/combat inputs; unobserved chips remain unknown rather than absent.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `screen_discovery.py`
- Inspect/modify: `transactions.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `planner.py`
- Create when absent: `guardian.py`
- Focused verification: `tests/test_guardian.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a06-manage-guardian-chips-and-materials` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_guardian.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_guardian.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `account_state.py` and `guardian.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_guardian.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a06-manage-guardian-chips-and-materials`, and open a PR against `main`.
