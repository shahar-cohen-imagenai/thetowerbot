# F04 — Route runs through maintenance

**Status:** [ ] Partial — resume
**Domain:** `farming`
**Dependencies:** [F02](./F02.md), [F03](./F03.md), [B06](./B06.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Choose retry versus home after death based on due work; arbitrate battle, Workshop, research, rewards and return navigation with a durable maintenance queue.

## Acceptance gate

Complete run → game over → home → due maintenance → next run without a manual tap; repeated RETRY cannot starve account upgrades.

## Current repository state

- [x] Autopilot recognizes run boundaries and has basic navigation/retry behavior.
- [ ] Still required: durable post-run maintenance arbitration that cannot starve account progression.

## File map

- Inspect/modify: `autopilot.py`
- Inspect/modify: `runner.py`
- Inspect/modify: `tower_bot.py`
- Create when absent: `maintenance.py`
- Focused verification: `tests/test_maintenance.py`
- Focused verification: `tests/test_autopilot.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/f04-route-runs-through-maintenance` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_maintenance.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_maintenance.py tests/test_autopilot.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `autopilot.py` and `maintenance.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_maintenance.py tests/test_autopilot.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/f04-route-runs-through-maintenance`, and open a PR against `main`.
