# C03 — Read primary and assist modules

**Status:** [ ] Not started
**Domain:** `cards_modules`
**Dependencies:** [B03](./B03.md), [B04](./B04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Inventory Cannon/Armor/Generator/Core and assist modules, equipped slots, levels, rarity, stars, unique effects, subeffects, shards, original natural-epic provenance, independent primary/assist levels, assist quantity flooring, banner/pity state and restrictions.

## Acceptance gate

Account state distinguishes unique-effect rarity scaling, main stats and primary/assist effects; every equipped module resolves to the correct category.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `account_state.py`
- Inspect/modify: `screen_discovery.py`
- Create when absent: `modules.py`
- Focused verification: `tests/test_modules.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/c03-read-primary-and-assist-modules` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_modules.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_modules.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `account_state.py` and `modules.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_modules.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/c03-read-primary-and-assist-modules`, and open a PR against `main`.
