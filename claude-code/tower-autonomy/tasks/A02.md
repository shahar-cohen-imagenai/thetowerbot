# A02 — Unlock and develop the Wall deliberately

**Status:** [ ] Partial — resume
**Domain:** `advanced`
**Dependencies:** [F03](./F03.md), [L04](./L04.md), [A01](./A01.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Treat existing Wall stat catalog entries as only a starting point; add readiness thresholds, Wall lab commitments, regen/fortification/thorns and health interactions.

## Acceptance gate

Wall is unlocked only after the configured full readiness predicate; required research and budget follow through instead of leaving a harmful half-built state.

## Current repository state

- [x] Wall concepts appear in upgrade and strategy data.
- [ ] Still required: deliberate unlock timing, labs/enhancements, survivability policy, and rollback-safe verification.

## File map

- Inspect/modify: `progression.py`
- Inspect/modify: `research_policy.py`
- Inspect/modify: `enhancements.py`
- Inspect/modify: `planner.py`
- Create when absent: `builds.py`
- Focused verification: `tests/test_wall_build.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/a02-unlock-and-develop-the-wall-deliberately` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_wall_build.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_wall_build.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `progression.py` and `builds.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_wall_build.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/a02-unlock-and-develop-the-wall-deliberately`, and open a PR against `main`.
