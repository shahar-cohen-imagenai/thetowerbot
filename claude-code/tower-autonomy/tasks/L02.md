# L02 — Run continuous research

**Status:** [ ] Not started
**Domain:** `labs`
**Dependencies:** [L01](./L01.md), [B05](./B05.md), [B07](./B07.md), [F04](./F04.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Unlock slots under the gem plan and execute start/queue/repeat/replace research with explicit prerequisites, completion confirmation and policy for interrupted jobs; configure and verify native auto-research/boost queues where unlocked without competing controllers.

## Acceptance gate

A lab finishing during battle is serviced through supported in-run menus or a policy-scheduled visit within the configured maintenance deadline; explicit intentional deferral is measured; coins remain reserved and research is not duplicated.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `shopping.py`
- Inspect/modify: `maintenance.py`
- Inspect/modify: `labs.py`
- Create when absent: `research_policy.py`
- Focused verification: `tests/test_research_policy.py`
- Focused verification: `tests/test_labs.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/l02-run-continuous-research` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_research_policy.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_research_policy.py tests/test_labs.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `shopping.py` and `research_policy.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_research_policy.py tests/test_labs.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/l02-run-continuous-research`, and open a PR against `main`.
