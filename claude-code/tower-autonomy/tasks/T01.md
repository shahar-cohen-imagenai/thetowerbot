# T01 — Complete daily and weekly objectives

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [B03](./B03.md), [B04](./B04.md), [B05](./B05.md), [F04](./F04.md), [F08](./F08.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read reset timers, objective progress and reward tracks; claim free rewards and schedule compatible actions with deduplication and missed-reset handling.

## Acceptance gate

Across a reset, completed rewards are claimed once and unfinished goals are carried or expired correctly without interrupting a more valuable run unnecessarily.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `maintenance.py`
- Inspect/modify: `interruptions.py`
- Inspect/modify: `transactions.py`
- Create when absent: `missions.py`
- Focused verification: `tests/test_missions.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t01-complete-daily-and-weekly-objectives` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_missions.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_missions.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `maintenance.py` and `missions.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_missions.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t01-complete-daily-and-weekly-objectives`, and open a PR against `main`.
