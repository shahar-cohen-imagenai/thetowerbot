# T02 — Plan events and event-shop spending

**Status:** [ ] Not started
**Domain:** `recurring`
**Dependencies:** [T01](./T01.md), [B07](./B07.md), [E05](./E05.md)
**Dependency gate now:** [ ] Blocked until every dependency above is complete

## Outcome

Read timed missions, event progress, shop inventory and prices; choose attainable goals and medal purchases, accounting for relic/theme priorities and rising purchase costs.

## Acceptance gate

Reserve event currency for the selected targets, execute compatible mission runs, claim milestones and stop at the configured completion/budget limit.

## Current repository state

- [ ] No graph-level implementation has been certified complete.

## File map

- Inspect/modify: `missions.py`
- Inspect/modify: `currencies.py`
- Inspect/modify: `planner.py`
- Create when absent: `rewards.py`
- Focused verification: `tests/test_events_policy.py`

## Execution checklist

- [ ] Confirm every dependency is marked `completed` in `../manifest.json`; stop if any dependency is incomplete.
- [ ] Create branch `codex/t02-plan-events-and-event-shop-spending` from current `origin/main` in a sibling worktree; never implement two graph tasks in one branch.
- [ ] Read `../CLAUDE.md`, the files above, and their focused tests. Use `rg` to locate symbols; do not scan broad test directories.
- [ ] Add failing tests in `tests/test_events_policy.py` for the acceptance gate, including success, unavailable, ambiguous, restart/retry, and no-action failure cases that apply to this task.
- [ ] Run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_events_policy.py -q` and confirm only the new or changed cases fail for the expected missing behavior.
- [ ] Implement the smallest complete change in `missions.py` and `rewards.py`; preserve unknown values, require fresh evidence before actions, and keep one verified device action per transaction step.
- [ ] Re-run `/Users/shahar/BifrostProjects/thetowerbot/.venv/bin/python -m pytest -p no:allure_pytest tests/test_events_policy.py -q` and require all focused checks to pass.
- [ ] If Python runtime or API behavior changed, run `npm run build --prefix web/ui` and verify `web/static/.build-manifest.json` matches the backend source hash.
- [ ] Update this task file and `../manifest.json` only after the acceptance gate is proven; record exact commands and evidence in the PR description.
- [ ] Stage only task-relevant source, tests, contracts, and fixtures. Show `git diff --cached --stat` and obtain the repository-required commit approval.
- [ ] Commit, verify `git branch -vv` does not target `origin/main`, push explicitly to `codex/t02-plan-events-and-event-shop-spending`, and open a PR against `main`.
